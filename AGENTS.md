# Agent instructions

`CLAUDE.md` in this directory holds them. It covers the repo layout, the code invariants and the build commands.

Nothing is duplicated here on purpose. Two files describing one repo drift, and then no reader can tell which one is current.
