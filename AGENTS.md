# Agent instructions

`CLAUDE.md` in this directory holds them. It covers the code invariants and the branch and merge conventions.

Nothing is duplicated here on purpose. Two files describing one repo drift, and then no reader can tell which one is current.
