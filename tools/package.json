{
  "name": "tools",
  "version": "0.0.0",
  "private": true,
  "description": "Build tooling. See CLAUDE.md repo layout.",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^5.0.0"
  }
}
